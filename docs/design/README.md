# Design reference screenshots

Capture a fixed 1180×760 window for each surface after a design phase lands. Pair before/after frames under this directory.

Suggested set:

- empty state
- file open
- split panes
- agent open
- agentic mode
- palette
- settings (Appearance)

Use `EDITUR_LOG=debug cargo run --release` and a consistent project so diffs stay honest.
